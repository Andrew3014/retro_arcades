# Copy to .env and fill with your values
# You can change PORT if 4000 is busy (e.g., 4001)
PORT=4000
JWT_SECRET=change_me_super_secret
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=retro_gaming
# Allow specific origins separated by comma. Localhost any port is always allowed by code.
CORS_ORIGIN=http://localhost:3000,http://localhost:5173
